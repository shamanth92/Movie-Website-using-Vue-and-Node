export default {
    popularMovies(state) {
        return state.popularMovies
    },
    nowPlaying(state) {
        return state.nowPlaying
    },
    popularTv(state) {
        return state.popularTv
    },
    videos(state) {
        return state.videos
    },
    movieDetails(state) {
        console.log(state.movieDetails)
        return state.movieDetails
    },
    movieCredits(state) {
        console.log(state.movieCredits)
        return state.movieCredits
    },
    tvDetails(state) {
        console.log(state.movieDetails)
        return state.tvDetails
    },
    tvCredits(state) {
        console.log(state.movieCredits)
        return state.tvCredits
    },
    topRatedMovies(state) {
        console.log(state.topRatedMovies)
        return state.topRatedMovies
    },
    upcomingMovies(state) {
        console.log(state.upcomingMovies)
        return state.upcomingMovies
    },
    likedMovies(state) {
        console.log(state.likedMovies)
        return state.likedMovies
    },
    watchlistMovies(state) {
        console.log(state.watchlistMovies)
        return state.watchlistMovies
    },
    likedTv(state) {
        console.log(state.likedTv)
        return state.likedTv
    },
    watchlistTv(state) {
        console.log(state.watchlistTv)
        return state.watchlistTv
    },
    searchedMovies(state) {
        console.log(state.searchedMovies)
        return state.searchedMovies
    },
    movieVideos(state) {
        console.log(state.movieVideos)
        return state.movieVideos
    },
    movieReviews(state) {
        console.log(state.movieReviews)
        return state.movieReviews
    },
}